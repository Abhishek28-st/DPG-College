user_string = input("Please enter a string: ")
if len(user_string) > 0:
    first_character = user_string[0]
    print(f"The first character of your string is: {first_character}")
else:
    print("You entered an empty string, so there is no first character to diplay.")
